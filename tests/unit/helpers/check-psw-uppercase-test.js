import { checkPswUppercase } from 'gallery/helpers/check-psw-uppercase';
import { module, test } from 'qunit';

module('Unit | Helper | check psw uppercase');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkPswUppercase([42]);
  assert.ok(result);
});
