import { confirmPassword } from 'gallery/helpers/confirm-password';
import { module, test } from 'qunit';

module('Unit | Helper | confirm password');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = confirmPassword([42]);
  assert.ok(result);
});
