import { removePatch } from 'gallery/helpers/remove-patch';
import { module, test } from 'qunit';

module('Unit | Helper | remove patch');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = removePatch([42]);
  assert.ok(result);
});
