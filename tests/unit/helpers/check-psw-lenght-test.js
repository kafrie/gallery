import { checkPswLenght } from 'gallery/helpers/check-psw-lenght';
import { module, test } from 'qunit';

module('Unit | Helper | check psw lenght');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkPswLenght([42]);
  assert.ok(result);
});
