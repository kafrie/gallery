import { checkPswNumericval } from 'gallery/helpers/check-psw-numericval';
import { module, test } from 'qunit';

module('Unit | Helper | check psw numericval');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkPswNumericval([42]);
  assert.ok(result);
});
