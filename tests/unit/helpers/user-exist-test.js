import { userExist } from 'gallery/helpers/user-exist';
import { module, test } from 'qunit';

module('Unit | Helper | user exist');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = userExist([42]);
  assert.ok(result);
});
