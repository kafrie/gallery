import { unverified } from 'gallery/helpers/unverified';
import { module, test } from 'qunit';

module('Unit | Helper | unverified');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = unverified([42]);
  assert.ok(result);
});
