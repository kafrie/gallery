import { checkPswSpecialchar } from 'gallery/helpers/check-psw-specialchar';
import { module, test } from 'qunit';

module('Unit | Helper | check psw specialchar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkPswSpecialchar([42]);
  assert.ok(result);
});
